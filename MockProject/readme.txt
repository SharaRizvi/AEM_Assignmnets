Components:
1. Header Componnet:
    Authorable logo and primary links
    multifield primary link (use of WCMUsePojo and bean class)
    Sublinks are links of child pages of primary links respectively (use of WCMUsePojo and bean class)
    required fileds
    validation of image and links via rootPath property
    use of clientlib
    
2. Banner Component:
    Authorable background image, heading, subheading and CTA link
    multifield carousel
    required and non-required fields
    validation of images and links via rootPath property
    use of clientlib
    
3. Trending News Component:
    Authorable image, heading, description, cta link and know more text
    required fields
    validation of image and link via rootPath property
    use of clientlib
